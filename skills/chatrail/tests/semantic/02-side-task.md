# Case 2: Short side task

## Case input

Mode: Root Stop review.

Latest user words:

```text
Keep PDF invoice import as the main goal. First rename the sample file. This is
a one-off side task, so do not add it to lasting history.
```

Useful tool proof: None. The rename has not started.

Rail:

```markdown
# Rail

## Entry: PDF import chosen

Basis: "The main goal is now PDF invoice import."

Meaning: PDF invoice import is the user-approved main goal.
```

Compass:

```markdown
# Compass

## Current heading

Build PDF invoice import.

## Relation to the latest user-approved direction

This is the main goal.

## Behind us

The PDF goal was chosen.

## Ahead

Build text extraction.

## Unclear

The sample file name is not settled.
```

## Pass signs

- `ACTION` is exactly `SAVE_COMPASS`.
- `ENTRY` is exactly `NONE`; no Rail entry is proposed.
- `COMPASS` has every required heading.
- The rename is the next side move, but PDF invoice import stays the main goal.
- The rename is not called done.
- `RETURN` is exactly `NONE`.
