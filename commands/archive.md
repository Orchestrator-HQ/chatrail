---
description: Archive this task's road to one file and start fresh — nothing is deleted
---
!`python3 "${CLAUDE_PLUGIN_ROOT}/skills/chatrail/scripts/chatrail.py" archive --thread-id "${CLAUDE_CODE_SESSION_ID}"`

Relay the script's one output line verbatim. Name the archive path it prints, and say the old rail and compass are kept in that file while the task starts fresh.
